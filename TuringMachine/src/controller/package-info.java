/**
 * This package contains the classes that control the machine, and machine UI
 * This also includes the java class for the custom font control component,
 * which serves as it's own controller and exists as a custom fxml based component
 */
package controller;
