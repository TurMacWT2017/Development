/**
 * The model package contains the classes that handle the various abstractions
 * of machine components, as well as the classes that process, tokenize, compile
 * and run provided programs.
 */
package model;
